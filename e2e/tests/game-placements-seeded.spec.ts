import { expect } from "@playwright/test";
import { test } from "./game-play-fixture";

test("moves and rotates ship", async () => {
  expect(1).toBe(1);
});

test("placements state is preserved after page refresh", async () => {
  expect(1).toBe(1);
});
